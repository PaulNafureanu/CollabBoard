import type { JsonPath, Json } from "./../shared/json";

export function applyPatchToPayload(currentPayload: Json, patch: { path: JsonPath; value: Json }): Json {
  //TODO: do stuff here :)

  return currentPayload; // for now
}
